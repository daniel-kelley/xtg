name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: prep
      run: |
        git clone --recurse-submodules https://github.com/daniel-kelley/xtg
    - name: pwd
      run: pwd
    - name: ls
      run: ls
    - name: find
      run: find . -type f
    - name: make
      run: make
    - name: make check
      run: |
        make clean
        make SANITIZE=address SANLIBS=-lasan check
